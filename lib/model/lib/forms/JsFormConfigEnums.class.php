<?php

class JsFormConfigEnums {
  
  /*
   * allow_extra_fields:  if false, the validator adds an error if extra fields are given in the input array 
   * of values (default to false)
   */
  const FORM_PARAM_PAGE = "page";

  /*
   * allow_extra_fields:  if false, the validator adds an error if extra fields are given in the input array 
   * of values (default to false)
   */
  const FORM_PARAM_VALUES = "formValues";
  /*
   * allow_extra_fields:  if false, the validator adds an error if extra fields are given in the input array 
   * of values (default to false)
   */
  const FORM_PARAM_HIDDEN_VALUES = "formHiddenValues";

  /*
   * allow_extra_fields:  if false, the validator adds an error if extra fields are given in the input array 
   * of values (default to false)
   */
  const FORM_PARAM_ALLOW_EXTRA_FIELDS = "allow_extra_fields";

  /*
   * CSRF Secret
   */
  
  const FORM_CSRF_SECRET = "JjJSsS_MySecReEeTttt!@!";
}

?>
