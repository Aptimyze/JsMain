select count(*), Date from Creative_Report group by Date; select count(*), Date from KeywordCriterion_Report group by Date; select count(*), Date from Campaign_Report group by Date; select count(*), Date from  AdGroup_Report group by Date;
